#page-root.page
  .page-root--left
    #repo-list.list(v-bind:class='classes')
      .list--header.pointer(v-on:click='update()')
        | Repositories
      .list--content(v-bind:style='styles')
        .list--item.pointer(v-for='item in items')
          | {{ item.name }}
      .list--loader(v-if='loading')
  .page-root--right
    | right
